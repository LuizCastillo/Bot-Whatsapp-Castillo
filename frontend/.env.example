# Somente servidor (NUNCA use prefixo NEXT_PUBLIC_ aqui). Nada disso chega ao navegador.
BACKEND_URL=http://localhost:8000
PROXY_SHARED_SECRET=troque-por-um-segredo-longo   # mesmo valor do backend
# Opcional: origens extras aceitas em requisições que alteram dados (ex.: domínio futuro)
ALLOWED_ORIGINS=
